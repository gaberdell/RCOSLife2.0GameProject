#include <fstream>
#include <iostream> // library for reading & writing from the console/keyboard
#include <cmath>    // library with the square root function & absolute value
#include <cstdlib>  // library with the exit function
#include <string>
#include <vector>
#include <sstream>
#include <cstring>
#include <list>
#include <iterator>

void recursiveSol(const std::vector<std::string>& valid_words, const std::vector<std::string>& invalid_words, std::vector<char**> grids ){

}

int main(int argc, char * argv[] ) {
    if(argc != 3) {
        std::cerr << "Incorrect amount of arguments. Check your command and try again" << std::endl;
    }

    if(argv.size() < 3){
       std::cerr << "Incorrect amount of arguments. Check your command and try again" << std::endl;
    }



}